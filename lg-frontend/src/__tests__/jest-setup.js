describe('Configuration and Setup tests', () => {
    it('should run', () => {
        expect(true).toBe(true);
    });

    it('should add two numbers', () => {
        expect(1 + 2).toBe(3);
    });
});
